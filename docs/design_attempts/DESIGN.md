# Zelora Design System

[file-tag: code-generated-file-0-1783077767636553988]

I have created a comprehensive, pixel-perfect design system inside a `DESIGN.md` file based on the uploaded UI layouts. It includes everything a coding agent needs to reproduce the design flawlessly, including:

* **Design Principles & Brand Tone** (Editorial, high-end fashion aesthetic)
* **Exact Colour Palette Tokens** (Hex values for main backgrounds, text, muted accents, and structural borders)
* **Typography Specification Scale** (Detailed CSS rules, serif/sans-serif pairings, font weights, and line heights)
* **Structural Layout Layouts** (Page boundaries, standard grids, and asymmetric hero breakdown)
* **Component Blueprints** (Specifications for Navigation headers, Buttons/CTAs, Infinite Marquees, Product Cards, and Review Modules)
* **Micro-Interactions & Styling Highlights** (Details regarding tilted ambient assets, highlighted text tokens, and badge behaviors)

## 1. Design Principles & Brand Tone

* Premium & Sophisticated editorial fashion layout.
* Asymmetric hero structures with overlapping decorative elements.

## 2. Color Palette & Typography Tokens

* Full color token mapping (`color-bg-main`, `color-bg-tint`, `color-accent-gold`, etc.).
* Exact Typography Scales mapping `Playfair Display` (serif) and `Inter` (sans-serif).

## 3. Structural Layout & Spacing

* Desktop container parameters (`max-width: 1440px`) and fluid clamp-based padding guidelines.
* 12-column grid and asymmetric split assignments for hero sections.

## 4. Component Specifications

* Global Navigation Header (Logo centering, action groups, bottom border definitions).
* Buttons & CTAs (Sizing, sharp borders, up-right arrows `↗`).
* Infinite Ticker / Marquee rules with custom separator glyphs (`✴`).
* Product / Collection Cards and Testimonial Cards.

## 5. Micro-Interactions & UI Elements

* Specific rotation parameters for ambient floating images (`transform: rotate(-12deg)`).
* Highlighted copy styling rules using `color-accent-gold`.
