// packages/ui/src/icons.ts

// REMEMBER: If you add new icons here, you must also add them to the `lucide-react` package's `icons.json` file, and then run `pnpm generate-icons` to regenerate the icon components.
export * from "lucide-react";
